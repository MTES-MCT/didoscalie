# Automatically generated by openapi-generator (https://openapi-generator.tech)
# Please update as you see appropriate

context("Test ReferentielsApi")

api.instance <- ReferentielsApi$new()

test_that("ExportCsvReferentielCog", {
  # tests for ExportCsvReferentielCog
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export csv \&quot;referentiel - cog\&quot;
  # Permet d&#39;obtenir un export csv d&#39;éléments de \&quot;referentiel - cog\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param with.column.name character faut-il ajouter le nom des colonnes en entête du fichier csv ?
  # @param with.column.description character faut-il ajouter la description des colonnes en entête du fichier csv ?
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: ID, MILLESIME, TYPE, LEVEL, CODE_MILLESIME, CODE_CURRENT, OFFICIAL_NAME, UPPER_NAME, LOWER_NAME, LONGITUDE, LATITUDE (optional)
  # @param ID character Pour filtrer selon la valeur de ID. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ID sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MILLESIME character Pour filtrer selon la valeur de MILLESIME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MILLESIME sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param TYPE character Pour filtrer selon la valeur de TYPE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour TYPE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LEVEL character Pour filtrer selon la valeur de LEVEL. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LEVEL sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE_MILLESIME character Pour filtrer selon la valeur de CODE_MILLESIME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE_MILLESIME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE_CURRENT character Pour filtrer selon la valeur de CODE_CURRENT. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE_CURRENT sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param OFFICIAL_NAME character Pour filtrer selon la valeur de OFFICIAL_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour OFFICIAL_NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param UPPER_NAME character Pour filtrer selon la valeur de UPPER_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour UPPER_NAME sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LOWER_NAME character Pour filtrer selon la valeur de LOWER_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOWER_NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LONGITUDE character Pour filtrer selon la valeur de LONGITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LONGITUDE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LATITUDE character Pour filtrer selon la valeur de LATITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LATITUDE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LOCATION character Pour filtrer selon la valeur de LOCATION. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOCATION sont: near, within (optional)
  # @param SHAPE character Pour filtrer selon la valeur de SHAPE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour SHAPE sont: near, within (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportCsvReferentielGeozones", {
  # tests for ExportCsvReferentielGeozones
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export csv \&quot;referentiel - geozones\&quot;
  # Permet d&#39;obtenir un export csv d&#39;éléments de \&quot;referentiel - geozones\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param with.column.name character faut-il ajouter le nom des colonnes en entête du fichier csv ?
  # @param with.column.description character faut-il ajouter la description des colonnes en entête du fichier csv ?
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: ID, LEVEL, CODE, VALIDITY_START, VALIDITY_END, NAME (optional)
  # @param ID character Pour filtrer selon la valeur de ID. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ID sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LEVEL character Pour filtrer selon la valeur de LEVEL. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LEVEL sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param VALIDITY_START character Pour filtrer selon la valeur de VALIDITY_START. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_START sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param VALIDITY_END character Pour filtrer selon la valeur de VALIDITY_END. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_END sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME character Pour filtrer selon la valeur de NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportCsvReferentielPolluantsEau", {
  # tests for ExportCsvReferentielPolluantsEau
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export csv \&quot;referentiel - polluantsEau\&quot;
  # Permet d&#39;obtenir un export csv d&#39;éléments de \&quot;referentiel - polluantsEau\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param with.column.name character faut-il ajouter le nom des colonnes en entête du fichier csv ?
  # @param with.column.description character faut-il ajouter la description des colonnes en entête du fichier csv ?
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, NAME, UNIT (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME character Pour filtrer selon la valeur de NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param UNIT character Pour filtrer selon la valeur de UNIT. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour UNIT sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportCsvReferentielPorts", {
  # tests for ExportCsvReferentielPorts
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export csv \&quot;referentiel - ports\&quot;
  # Permet d&#39;obtenir un export csv d&#39;éléments de \&quot;referentiel - ports\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param with.column.name character faut-il ajouter le nom des colonnes en entête du fichier csv ?
  # @param with.column.description character faut-il ajouter la description des colonnes en entête du fichier csv ?
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, NAME, MCA_CODE, MCA_NAME (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME character Pour filtrer selon la valeur de NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MCA_CODE character Pour filtrer selon la valeur de MCA_CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MCA_CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MCA_NAME character Pour filtrer selon la valeur de MCA_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MCA_NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportCsvReferentielStationsAir", {
  # tests for ExportCsvReferentielStationsAir
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export csv \&quot;referentiel - stationsAir\&quot;
  # Permet d&#39;obtenir un export csv d&#39;éléments de \&quot;referentiel - stationsAir\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param with.column.name character faut-il ajouter le nom des colonnes en entête du fichier csv ?
  # @param with.column.description character faut-il ajouter la description des colonnes en entête du fichier csv ?
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, NAME, CODE_ZAS, NAME_ZAS, COMMUNE, AASQA, LONGITUDE, LATITUDE, ALTITUDE, VALIDITY_START, VALIDITY_END, SECTOR_TYPE, MILLESIME, IMPACT_PM10, IMPACT_NO2, IMPACT_O3, IMPACT_SO2, IMPACT_PM25, IMPACT_CO (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME character Pour filtrer selon la valeur de NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE_ZAS character Pour filtrer selon la valeur de CODE_ZAS. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE_ZAS sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME_ZAS character Pour filtrer selon la valeur de NAME_ZAS. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME_ZAS sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param COMMUNE character Pour filtrer selon la valeur de COMMUNE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour COMMUNE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param AASQA character Pour filtrer selon la valeur de AASQA. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour AASQA sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LONGITUDE character Pour filtrer selon la valeur de LONGITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LONGITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LATITUDE character Pour filtrer selon la valeur de LATITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LATITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param ALTITUDE character Pour filtrer selon la valeur de ALTITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ALTITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param VALIDITY_START character Pour filtrer selon la valeur de VALIDITY_START. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_START sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param VALIDITY_END character Pour filtrer selon la valeur de VALIDITY_END. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_END sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param SECTOR_TYPE character Pour filtrer selon la valeur de SECTOR_TYPE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour SECTOR_TYPE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MILLESIME character Pour filtrer selon la valeur de MILLESIME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MILLESIME sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param impact.pm10 character Pour filtrer selon la valeur de IMPACT_PM10. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_PM10 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.no2 character Pour filtrer selon la valeur de IMPACT_NO2. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_NO2 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.o3 character Pour filtrer selon la valeur de IMPACT_O3. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_O3 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.so2 character Pour filtrer selon la valeur de IMPACT_SO2. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_SO2 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.pm25 character Pour filtrer selon la valeur de IMPACT_PM25. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_PM25 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param IMPACT_CO character Pour filtrer selon la valeur de IMPACT_CO. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_CO sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LOCATION character Pour filtrer selon la valeur de LOCATION. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOCATION sont: near, within (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportCsvReferentielStationsEsu", {
  # tests for ExportCsvReferentielStationsEsu
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export csv \&quot;referentiel - stationsEsu\&quot;
  # Permet d&#39;obtenir un export csv d&#39;éléments de \&quot;referentiel - stationsEsu\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param with.column.name character faut-il ajouter le nom des colonnes en entête du fichier csv ?
  # @param with.column.description character faut-il ajouter la description des colonnes en entête du fichier csv ?
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, LONGITUDE, LATITUDE (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LONGITUDE character Pour filtrer selon la valeur de LONGITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LONGITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LATITUDE character Pour filtrer selon la valeur de LATITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LATITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LOCATION character Pour filtrer selon la valeur de LOCATION. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOCATION sont: near, within (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportCsvReferentielTags", {
  # tests for ExportCsvReferentielTags
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export csv \&quot;referentiel - tags\&quot;
  # Permet d&#39;obtenir un export csv d&#39;éléments de \&quot;referentiel - tags\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param with.column.name character faut-il ajouter le nom des colonnes en entête du fichier csv ?
  # @param with.column.description character faut-il ajouter la description des colonnes en entête du fichier csv ?
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: VALUE, DISPLAY, TOPICS, EUROVOC, ECOPLANET (optional)
  # @param VALUE character Pour filtrer selon la valeur de VALUE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALUE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param DISPLAY character Pour filtrer selon la valeur de DISPLAY. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour DISPLAY sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param TOPICS character Pour filtrer selon la valeur de TOPICS. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour TOPICS sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param EUROVOC character Pour filtrer selon la valeur de EUROVOC. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour EUROVOC sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param ECOPLANET character Pour filtrer selon la valeur de ECOPLANET. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ECOPLANET sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportGeojsonReferentielCog", {
  # tests for ExportGeojsonReferentielCog
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export geojson \&quot;referentiel - cog\&quot;
  # Permet d&#39;obtenir un export geojson d&#39;éléments de \&quot;referentiel - cog\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param geo.field character Champ pivot pour la génération du geojson ou du shapefile
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: ID, MILLESIME, TYPE, LEVEL, CODE_MILLESIME, CODE_CURRENT, OFFICIAL_NAME, UPPER_NAME, LOWER_NAME, LONGITUDE, LATITUDE (optional)
  # @param ID character Pour filtrer selon la valeur de ID. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ID sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MILLESIME character Pour filtrer selon la valeur de MILLESIME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MILLESIME sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param TYPE character Pour filtrer selon la valeur de TYPE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour TYPE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LEVEL character Pour filtrer selon la valeur de LEVEL. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LEVEL sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE_MILLESIME character Pour filtrer selon la valeur de CODE_MILLESIME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE_MILLESIME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE_CURRENT character Pour filtrer selon la valeur de CODE_CURRENT. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE_CURRENT sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param OFFICIAL_NAME character Pour filtrer selon la valeur de OFFICIAL_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour OFFICIAL_NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param UPPER_NAME character Pour filtrer selon la valeur de UPPER_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour UPPER_NAME sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LOWER_NAME character Pour filtrer selon la valeur de LOWER_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOWER_NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LONGITUDE character Pour filtrer selon la valeur de LONGITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LONGITUDE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LATITUDE character Pour filtrer selon la valeur de LATITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LATITUDE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LOCATION character Pour filtrer selon la valeur de LOCATION. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOCATION sont: near, within (optional)
  # @param SHAPE character Pour filtrer selon la valeur de SHAPE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour SHAPE sont: near, within (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportGeojsonReferentielStationsAir", {
  # tests for ExportGeojsonReferentielStationsAir
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export geojson \&quot;referentiel - stationsAir\&quot;
  # Permet d&#39;obtenir un export geojson d&#39;éléments de \&quot;referentiel - stationsAir\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param geo.field character Champ pivot pour la génération du geojson ou du shapefile
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, NAME, CODE_ZAS, NAME_ZAS, COMMUNE, AASQA, LONGITUDE, LATITUDE, ALTITUDE, VALIDITY_START, VALIDITY_END, SECTOR_TYPE, MILLESIME, IMPACT_PM10, IMPACT_NO2, IMPACT_O3, IMPACT_SO2, IMPACT_PM25, IMPACT_CO (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME character Pour filtrer selon la valeur de NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE_ZAS character Pour filtrer selon la valeur de CODE_ZAS. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE_ZAS sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME_ZAS character Pour filtrer selon la valeur de NAME_ZAS. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME_ZAS sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param COMMUNE character Pour filtrer selon la valeur de COMMUNE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour COMMUNE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param AASQA character Pour filtrer selon la valeur de AASQA. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour AASQA sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LONGITUDE character Pour filtrer selon la valeur de LONGITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LONGITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LATITUDE character Pour filtrer selon la valeur de LATITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LATITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param ALTITUDE character Pour filtrer selon la valeur de ALTITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ALTITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param VALIDITY_START character Pour filtrer selon la valeur de VALIDITY_START. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_START sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param VALIDITY_END character Pour filtrer selon la valeur de VALIDITY_END. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_END sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param SECTOR_TYPE character Pour filtrer selon la valeur de SECTOR_TYPE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour SECTOR_TYPE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MILLESIME character Pour filtrer selon la valeur de MILLESIME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MILLESIME sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param impact.pm10 character Pour filtrer selon la valeur de IMPACT_PM10. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_PM10 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.no2 character Pour filtrer selon la valeur de IMPACT_NO2. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_NO2 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.o3 character Pour filtrer selon la valeur de IMPACT_O3. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_O3 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.so2 character Pour filtrer selon la valeur de IMPACT_SO2. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_SO2 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.pm25 character Pour filtrer selon la valeur de IMPACT_PM25. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_PM25 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param IMPACT_CO character Pour filtrer selon la valeur de IMPACT_CO. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_CO sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LOCATION character Pour filtrer selon la valeur de LOCATION. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOCATION sont: near, within (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportGeojsonReferentielStationsEsu", {
  # tests for ExportGeojsonReferentielStationsEsu
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export geojson \&quot;referentiel - stationsEsu\&quot;
  # Permet d&#39;obtenir un export geojson d&#39;éléments de \&quot;referentiel - stationsEsu\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param geo.field character Champ pivot pour la génération du geojson ou du shapefile
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, LONGITUDE, LATITUDE (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LONGITUDE character Pour filtrer selon la valeur de LONGITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LONGITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LATITUDE character Pour filtrer selon la valeur de LATITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LATITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LOCATION character Pour filtrer selon la valeur de LOCATION. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOCATION sont: near, within (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportJsonReferentielCog", {
  # tests for ExportJsonReferentielCog
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export json \&quot;referentiel - cog\&quot;
  # Permet d&#39;obtenir un export json d&#39;éléments de \&quot;referentiel - cog\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: ID, MILLESIME, TYPE, LEVEL, CODE_MILLESIME, CODE_CURRENT, OFFICIAL_NAME, UPPER_NAME, LOWER_NAME, LONGITUDE, LATITUDE (optional)
  # @param ID character Pour filtrer selon la valeur de ID. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ID sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MILLESIME character Pour filtrer selon la valeur de MILLESIME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MILLESIME sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param TYPE character Pour filtrer selon la valeur de TYPE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour TYPE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LEVEL character Pour filtrer selon la valeur de LEVEL. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LEVEL sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE_MILLESIME character Pour filtrer selon la valeur de CODE_MILLESIME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE_MILLESIME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE_CURRENT character Pour filtrer selon la valeur de CODE_CURRENT. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE_CURRENT sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param OFFICIAL_NAME character Pour filtrer selon la valeur de OFFICIAL_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour OFFICIAL_NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param UPPER_NAME character Pour filtrer selon la valeur de UPPER_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour UPPER_NAME sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LOWER_NAME character Pour filtrer selon la valeur de LOWER_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOWER_NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LONGITUDE character Pour filtrer selon la valeur de LONGITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LONGITUDE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LATITUDE character Pour filtrer selon la valeur de LATITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LATITUDE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LOCATION character Pour filtrer selon la valeur de LOCATION. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOCATION sont: near, within (optional)
  # @param SHAPE character Pour filtrer selon la valeur de SHAPE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour SHAPE sont: near, within (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportJsonReferentielGeozones", {
  # tests for ExportJsonReferentielGeozones
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export json \&quot;referentiel - geozones\&quot;
  # Permet d&#39;obtenir un export json d&#39;éléments de \&quot;referentiel - geozones\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: ID, LEVEL, CODE, VALIDITY_START, VALIDITY_END, NAME (optional)
  # @param ID character Pour filtrer selon la valeur de ID. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ID sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LEVEL character Pour filtrer selon la valeur de LEVEL. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LEVEL sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param VALIDITY_START character Pour filtrer selon la valeur de VALIDITY_START. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_START sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param VALIDITY_END character Pour filtrer selon la valeur de VALIDITY_END. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_END sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME character Pour filtrer selon la valeur de NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportJsonReferentielPolluantsEau", {
  # tests for ExportJsonReferentielPolluantsEau
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export json \&quot;referentiel - polluantsEau\&quot;
  # Permet d&#39;obtenir un export json d&#39;éléments de \&quot;referentiel - polluantsEau\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, NAME, UNIT (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME character Pour filtrer selon la valeur de NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param UNIT character Pour filtrer selon la valeur de UNIT. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour UNIT sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportJsonReferentielPorts", {
  # tests for ExportJsonReferentielPorts
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export json \&quot;referentiel - ports\&quot;
  # Permet d&#39;obtenir un export json d&#39;éléments de \&quot;referentiel - ports\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, NAME, MCA_CODE, MCA_NAME (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME character Pour filtrer selon la valeur de NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MCA_CODE character Pour filtrer selon la valeur de MCA_CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MCA_CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MCA_NAME character Pour filtrer selon la valeur de MCA_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MCA_NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportJsonReferentielStationsAir", {
  # tests for ExportJsonReferentielStationsAir
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export json \&quot;referentiel - stationsAir\&quot;
  # Permet d&#39;obtenir un export json d&#39;éléments de \&quot;referentiel - stationsAir\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, NAME, CODE_ZAS, NAME_ZAS, COMMUNE, AASQA, LONGITUDE, LATITUDE, ALTITUDE, VALIDITY_START, VALIDITY_END, SECTOR_TYPE, MILLESIME, IMPACT_PM10, IMPACT_NO2, IMPACT_O3, IMPACT_SO2, IMPACT_PM25, IMPACT_CO (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME character Pour filtrer selon la valeur de NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE_ZAS character Pour filtrer selon la valeur de CODE_ZAS. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE_ZAS sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME_ZAS character Pour filtrer selon la valeur de NAME_ZAS. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME_ZAS sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param COMMUNE character Pour filtrer selon la valeur de COMMUNE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour COMMUNE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param AASQA character Pour filtrer selon la valeur de AASQA. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour AASQA sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LONGITUDE character Pour filtrer selon la valeur de LONGITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LONGITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LATITUDE character Pour filtrer selon la valeur de LATITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LATITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param ALTITUDE character Pour filtrer selon la valeur de ALTITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ALTITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param VALIDITY_START character Pour filtrer selon la valeur de VALIDITY_START. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_START sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param VALIDITY_END character Pour filtrer selon la valeur de VALIDITY_END. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_END sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param SECTOR_TYPE character Pour filtrer selon la valeur de SECTOR_TYPE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour SECTOR_TYPE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MILLESIME character Pour filtrer selon la valeur de MILLESIME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MILLESIME sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param impact.pm10 character Pour filtrer selon la valeur de IMPACT_PM10. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_PM10 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.no2 character Pour filtrer selon la valeur de IMPACT_NO2. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_NO2 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.o3 character Pour filtrer selon la valeur de IMPACT_O3. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_O3 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.so2 character Pour filtrer selon la valeur de IMPACT_SO2. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_SO2 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.pm25 character Pour filtrer selon la valeur de IMPACT_PM25. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_PM25 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param IMPACT_CO character Pour filtrer selon la valeur de IMPACT_CO. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_CO sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LOCATION character Pour filtrer selon la valeur de LOCATION. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOCATION sont: near, within (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportJsonReferentielStationsEsu", {
  # tests for ExportJsonReferentielStationsEsu
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export json \&quot;referentiel - stationsEsu\&quot;
  # Permet d&#39;obtenir un export json d&#39;éléments de \&quot;referentiel - stationsEsu\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, LONGITUDE, LATITUDE (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LONGITUDE character Pour filtrer selon la valeur de LONGITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LONGITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LATITUDE character Pour filtrer selon la valeur de LATITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LATITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LOCATION character Pour filtrer selon la valeur de LOCATION. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOCATION sont: near, within (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportJsonReferentielTags", {
  # tests for ExportJsonReferentielTags
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export json \&quot;referentiel - tags\&quot;
  # Permet d&#39;obtenir un export json d&#39;éléments de \&quot;referentiel - tags\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: VALUE, DISPLAY, TOPICS, EUROVOC, ECOPLANET (optional)
  # @param VALUE character Pour filtrer selon la valeur de VALUE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALUE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param DISPLAY character Pour filtrer selon la valeur de DISPLAY. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour DISPLAY sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param TOPICS character Pour filtrer selon la valeur de TOPICS. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour TOPICS sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param EUROVOC character Pour filtrer selon la valeur de EUROVOC. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour EUROVOC sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param ECOPLANET character Pour filtrer selon la valeur de ECOPLANET. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ECOPLANET sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @return [character]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportShapefileReferentielCog", {
  # tests for ExportShapefileReferentielCog
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export shapefile \&quot;referentiel - cog\&quot;
  # Permet d&#39;obtenir un export shapefile d&#39;éléments de \&quot;referentiel - cog\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param geo.field character Champ pivot pour la génération du geojson ou du shapefile
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: ID, MILLESIME, TYPE, LEVEL, CODE_MILLESIME, CODE_CURRENT, OFFICIAL_NAME, UPPER_NAME, LOWER_NAME, LONGITUDE, LATITUDE (optional)
  # @param ID character Pour filtrer selon la valeur de ID. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ID sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MILLESIME character Pour filtrer selon la valeur de MILLESIME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MILLESIME sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param TYPE character Pour filtrer selon la valeur de TYPE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour TYPE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LEVEL character Pour filtrer selon la valeur de LEVEL. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LEVEL sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE_MILLESIME character Pour filtrer selon la valeur de CODE_MILLESIME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE_MILLESIME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE_CURRENT character Pour filtrer selon la valeur de CODE_CURRENT. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE_CURRENT sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param OFFICIAL_NAME character Pour filtrer selon la valeur de OFFICIAL_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour OFFICIAL_NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param UPPER_NAME character Pour filtrer selon la valeur de UPPER_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour UPPER_NAME sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LOWER_NAME character Pour filtrer selon la valeur de LOWER_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOWER_NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LONGITUDE character Pour filtrer selon la valeur de LONGITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LONGITUDE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LATITUDE character Pour filtrer selon la valeur de LATITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LATITUDE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LOCATION character Pour filtrer selon la valeur de LOCATION. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOCATION sont: near, within (optional)
  # @param SHAPE character Pour filtrer selon la valeur de SHAPE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour SHAPE sont: near, within (optional)
  # @return [data.frame]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportShapefileReferentielStationsAir", {
  # tests for ExportShapefileReferentielStationsAir
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export shapefile \&quot;referentiel - stationsAir\&quot;
  # Permet d&#39;obtenir un export shapefile d&#39;éléments de \&quot;referentiel - stationsAir\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param geo.field character Champ pivot pour la génération du geojson ou du shapefile
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, NAME, CODE_ZAS, NAME_ZAS, COMMUNE, AASQA, LONGITUDE, LATITUDE, ALTITUDE, VALIDITY_START, VALIDITY_END, SECTOR_TYPE, MILLESIME, IMPACT_PM10, IMPACT_NO2, IMPACT_O3, IMPACT_SO2, IMPACT_PM25, IMPACT_CO (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME character Pour filtrer selon la valeur de NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE_ZAS character Pour filtrer selon la valeur de CODE_ZAS. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE_ZAS sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME_ZAS character Pour filtrer selon la valeur de NAME_ZAS. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME_ZAS sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param COMMUNE character Pour filtrer selon la valeur de COMMUNE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour COMMUNE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param AASQA character Pour filtrer selon la valeur de AASQA. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour AASQA sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LONGITUDE character Pour filtrer selon la valeur de LONGITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LONGITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LATITUDE character Pour filtrer selon la valeur de LATITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LATITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param ALTITUDE character Pour filtrer selon la valeur de ALTITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ALTITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param VALIDITY_START character Pour filtrer selon la valeur de VALIDITY_START. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_START sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param VALIDITY_END character Pour filtrer selon la valeur de VALIDITY_END. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_END sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param SECTOR_TYPE character Pour filtrer selon la valeur de SECTOR_TYPE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour SECTOR_TYPE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MILLESIME character Pour filtrer selon la valeur de MILLESIME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MILLESIME sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param impact.pm10 character Pour filtrer selon la valeur de IMPACT_PM10. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_PM10 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.no2 character Pour filtrer selon la valeur de IMPACT_NO2. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_NO2 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.o3 character Pour filtrer selon la valeur de IMPACT_O3. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_O3 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.so2 character Pour filtrer selon la valeur de IMPACT_SO2. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_SO2 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.pm25 character Pour filtrer selon la valeur de IMPACT_PM25. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_PM25 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param IMPACT_CO character Pour filtrer selon la valeur de IMPACT_CO. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_CO sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LOCATION character Pour filtrer selon la valeur de LOCATION. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOCATION sont: near, within (optional)
  # @return [data.frame]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportShapefileReferentielStationsEsu", {
  # tests for ExportShapefileReferentielStationsEsu
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export shapefile \&quot;referentiel - stationsEsu\&quot;
  # Permet d&#39;obtenir un export shapefile d&#39;éléments de \&quot;referentiel - stationsEsu\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param geo.field character Champ pivot pour la génération du geojson ou du shapefile
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, LONGITUDE, LATITUDE (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LONGITUDE character Pour filtrer selon la valeur de LONGITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LONGITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LATITUDE character Pour filtrer selon la valeur de LATITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LATITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LOCATION character Pour filtrer selon la valeur de LOCATION. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOCATION sont: near, within (optional)
  # @return [data.frame]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportXlsxReferentielCog", {
  # tests for ExportXlsxReferentielCog
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export xlsx \&quot;referentiel - cog\&quot;
  # Permet d&#39;obtenir un export xlsx d&#39;éléments de \&quot;referentiel - cog\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param with.column.name character faut-il ajouter le nom des colonnes en entête du fichier csv ?
  # @param with.column.description character faut-il ajouter la description des colonnes en entête du fichier csv ?
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: ID, MILLESIME, TYPE, LEVEL, CODE_MILLESIME, CODE_CURRENT, OFFICIAL_NAME, UPPER_NAME, LOWER_NAME, LONGITUDE, LATITUDE (optional)
  # @param ID character Pour filtrer selon la valeur de ID. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ID sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MILLESIME character Pour filtrer selon la valeur de MILLESIME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MILLESIME sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param TYPE character Pour filtrer selon la valeur de TYPE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour TYPE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LEVEL character Pour filtrer selon la valeur de LEVEL. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LEVEL sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE_MILLESIME character Pour filtrer selon la valeur de CODE_MILLESIME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE_MILLESIME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE_CURRENT character Pour filtrer selon la valeur de CODE_CURRENT. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE_CURRENT sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param OFFICIAL_NAME character Pour filtrer selon la valeur de OFFICIAL_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour OFFICIAL_NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param UPPER_NAME character Pour filtrer selon la valeur de UPPER_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour UPPER_NAME sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LOWER_NAME character Pour filtrer selon la valeur de LOWER_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOWER_NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LONGITUDE character Pour filtrer selon la valeur de LONGITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LONGITUDE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LATITUDE character Pour filtrer selon la valeur de LATITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LATITUDE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LOCATION character Pour filtrer selon la valeur de LOCATION. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOCATION sont: near, within (optional)
  # @param SHAPE character Pour filtrer selon la valeur de SHAPE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour SHAPE sont: near, within (optional)
  # @return [data.frame]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportXlsxReferentielGeozones", {
  # tests for ExportXlsxReferentielGeozones
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export xlsx \&quot;referentiel - geozones\&quot;
  # Permet d&#39;obtenir un export xlsx d&#39;éléments de \&quot;referentiel - geozones\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param with.column.name character faut-il ajouter le nom des colonnes en entête du fichier csv ?
  # @param with.column.description character faut-il ajouter la description des colonnes en entête du fichier csv ?
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: ID, LEVEL, CODE, VALIDITY_START, VALIDITY_END, NAME (optional)
  # @param ID character Pour filtrer selon la valeur de ID. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ID sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LEVEL character Pour filtrer selon la valeur de LEVEL. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LEVEL sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param VALIDITY_START character Pour filtrer selon la valeur de VALIDITY_START. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_START sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param VALIDITY_END character Pour filtrer selon la valeur de VALIDITY_END. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_END sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME character Pour filtrer selon la valeur de NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @return [data.frame]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportXlsxReferentielPolluantsEau", {
  # tests for ExportXlsxReferentielPolluantsEau
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export xlsx \&quot;referentiel - polluantsEau\&quot;
  # Permet d&#39;obtenir un export xlsx d&#39;éléments de \&quot;referentiel - polluantsEau\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param with.column.name character faut-il ajouter le nom des colonnes en entête du fichier csv ?
  # @param with.column.description character faut-il ajouter la description des colonnes en entête du fichier csv ?
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, NAME, UNIT (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME character Pour filtrer selon la valeur de NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param UNIT character Pour filtrer selon la valeur de UNIT. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour UNIT sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @return [data.frame]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportXlsxReferentielPorts", {
  # tests for ExportXlsxReferentielPorts
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export xlsx \&quot;referentiel - ports\&quot;
  # Permet d&#39;obtenir un export xlsx d&#39;éléments de \&quot;referentiel - ports\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param with.column.name character faut-il ajouter le nom des colonnes en entête du fichier csv ?
  # @param with.column.description character faut-il ajouter la description des colonnes en entête du fichier csv ?
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, NAME, MCA_CODE, MCA_NAME (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME character Pour filtrer selon la valeur de NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MCA_CODE character Pour filtrer selon la valeur de MCA_CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MCA_CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MCA_NAME character Pour filtrer selon la valeur de MCA_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MCA_NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @return [data.frame]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportXlsxReferentielStationsAir", {
  # tests for ExportXlsxReferentielStationsAir
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export xlsx \&quot;referentiel - stationsAir\&quot;
  # Permet d&#39;obtenir un export xlsx d&#39;éléments de \&quot;referentiel - stationsAir\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param with.column.name character faut-il ajouter le nom des colonnes en entête du fichier csv ?
  # @param with.column.description character faut-il ajouter la description des colonnes en entête du fichier csv ?
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, NAME, CODE_ZAS, NAME_ZAS, COMMUNE, AASQA, LONGITUDE, LATITUDE, ALTITUDE, VALIDITY_START, VALIDITY_END, SECTOR_TYPE, MILLESIME, IMPACT_PM10, IMPACT_NO2, IMPACT_O3, IMPACT_SO2, IMPACT_PM25, IMPACT_CO (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME character Pour filtrer selon la valeur de NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE_ZAS character Pour filtrer selon la valeur de CODE_ZAS. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE_ZAS sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME_ZAS character Pour filtrer selon la valeur de NAME_ZAS. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME_ZAS sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param COMMUNE character Pour filtrer selon la valeur de COMMUNE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour COMMUNE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param AASQA character Pour filtrer selon la valeur de AASQA. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour AASQA sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LONGITUDE character Pour filtrer selon la valeur de LONGITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LONGITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LATITUDE character Pour filtrer selon la valeur de LATITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LATITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param ALTITUDE character Pour filtrer selon la valeur de ALTITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ALTITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param VALIDITY_START character Pour filtrer selon la valeur de VALIDITY_START. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_START sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param VALIDITY_END character Pour filtrer selon la valeur de VALIDITY_END. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_END sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param SECTOR_TYPE character Pour filtrer selon la valeur de SECTOR_TYPE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour SECTOR_TYPE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MILLESIME character Pour filtrer selon la valeur de MILLESIME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MILLESIME sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param impact.pm10 character Pour filtrer selon la valeur de IMPACT_PM10. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_PM10 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.no2 character Pour filtrer selon la valeur de IMPACT_NO2. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_NO2 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.o3 character Pour filtrer selon la valeur de IMPACT_O3. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_O3 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.so2 character Pour filtrer selon la valeur de IMPACT_SO2. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_SO2 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.pm25 character Pour filtrer selon la valeur de IMPACT_PM25. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_PM25 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param IMPACT_CO character Pour filtrer selon la valeur de IMPACT_CO. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_CO sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LOCATION character Pour filtrer selon la valeur de LOCATION. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOCATION sont: near, within (optional)
  # @return [data.frame]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportXlsxReferentielStationsEsu", {
  # tests for ExportXlsxReferentielStationsEsu
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export xlsx \&quot;referentiel - stationsEsu\&quot;
  # Permet d&#39;obtenir un export xlsx d&#39;éléments de \&quot;referentiel - stationsEsu\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param with.column.name character faut-il ajouter le nom des colonnes en entête du fichier csv ?
  # @param with.column.description character faut-il ajouter la description des colonnes en entête du fichier csv ?
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, LONGITUDE, LATITUDE (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LONGITUDE character Pour filtrer selon la valeur de LONGITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LONGITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LATITUDE character Pour filtrer selon la valeur de LATITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LATITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LOCATION character Pour filtrer selon la valeur de LOCATION. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOCATION sont: near, within (optional)
  # @return [data.frame]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ExportXlsxReferentielTags", {
  # tests for ExportXlsxReferentielTags
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Export xlsx \&quot;referentiel - tags\&quot;
  # Permet d&#39;obtenir un export xlsx d&#39;éléments de \&quot;referentiel - tags\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param with.column.name character faut-il ajouter le nom des colonnes en entête du fichier csv ?
  # @param with.column.description character faut-il ajouter la description des colonnes en entête du fichier csv ?
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: VALUE, DISPLAY, TOPICS, EUROVOC, ECOPLANET (optional)
  # @param VALUE character Pour filtrer selon la valeur de VALUE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALUE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param DISPLAY character Pour filtrer selon la valeur de DISPLAY. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour DISPLAY sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param TOPICS character Pour filtrer selon la valeur de TOPICS. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour TOPICS sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param EUROVOC character Pour filtrer selon la valeur de EUROVOC. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour EUROVOC sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param ECOPLANET character Pour filtrer selon la valeur de ECOPLANET. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ECOPLANET sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @return [data.frame]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetIdReferentielCog", {
  # tests for GetIdReferentielCog
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Détail de l&#39;élément avec l&#39;identifiant :id de \&quot;referentiel - cog\&quot;
  # Permet d&#39;obtenir le détail d&#39;un élément de \&quot;referentiel - cog\&quot; dont on connaît l&#39;id.
  # @param id character identifiant de l'élement dans \"referentiel - cog\"
  # @return [ItemCog]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetIdReferentielGeozones", {
  # tests for GetIdReferentielGeozones
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Détail de l&#39;élément avec l&#39;identifiant :id de \&quot;referentiel - geozones\&quot;
  # Permet d&#39;obtenir le détail d&#39;un élément de \&quot;referentiel - geozones\&quot; dont on connaît l&#39;id.
  # @param id character identifiant de l'élement dans \"referentiel - geozones\"
  # @return [ItemGeozones]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetIdReferentielPolluantsEau", {
  # tests for GetIdReferentielPolluantsEau
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Détail de l&#39;élément avec l&#39;identifiant :id de \&quot;referentiel - polluantsEau\&quot;
  # Permet d&#39;obtenir le détail d&#39;un élément de \&quot;referentiel - polluantsEau\&quot; dont on connaît l&#39;id.
  # @param id character identifiant de l'élement dans \"referentiel - polluantsEau\"
  # @return [ItemPolluantsEau]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetIdReferentielPorts", {
  # tests for GetIdReferentielPorts
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Détail de l&#39;élément avec l&#39;identifiant :id de \&quot;referentiel - ports\&quot;
  # Permet d&#39;obtenir le détail d&#39;un élément de \&quot;referentiel - ports\&quot; dont on connaît l&#39;id.
  # @param id character identifiant de l'élement dans \"referentiel - ports\"
  # @return [ItemPorts]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetIdReferentielStationsAir", {
  # tests for GetIdReferentielStationsAir
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Détail de l&#39;élément avec l&#39;identifiant :id de \&quot;referentiel - stationsAir\&quot;
  # Permet d&#39;obtenir le détail d&#39;un élément de \&quot;referentiel - stationsAir\&quot; dont on connaît l&#39;id.
  # @param id character identifiant de l'élement dans \"referentiel - stationsAir\"
  # @return [ItemStationsAir]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetIdReferentielStationsEsu", {
  # tests for GetIdReferentielStationsEsu
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Détail de l&#39;élément avec l&#39;identifiant :id de \&quot;referentiel - stationsEsu\&quot;
  # Permet d&#39;obtenir le détail d&#39;un élément de \&quot;referentiel - stationsEsu\&quot; dont on connaît l&#39;id.
  # @param id character identifiant de l'élement dans \"referentiel - stationsEsu\"
  # @return [ItemStationsEsu]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetIdReferentielTags", {
  # tests for GetIdReferentielTags
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Détail de l&#39;élément avec l&#39;identifiant :id de \&quot;referentiel - tags\&quot;
  # Permet d&#39;obtenir le détail d&#39;un élément de \&quot;referentiel - tags\&quot; dont on connaît l&#39;id.
  # @param id character identifiant de l'élement dans \"referentiel - tags\"
  # @return [ItemTags]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("PaginateReferentielCog", {
  # tests for PaginateReferentielCog
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Pagination \&quot;referentiel - cog\&quot;
  # Permet d&#39;obtenir une liste paginée d&#39;éléments de \&quot;referentiel - cog\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param page integer Numéro de la page à afficher
  # @param page.size character Nombre de lignes par page
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: ID, MILLESIME, TYPE, LEVEL, CODE_MILLESIME, CODE_CURRENT, OFFICIAL_NAME, UPPER_NAME, LOWER_NAME, LONGITUDE, LATITUDE (optional)
  # @param ID character Pour filtrer selon la valeur de ID. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ID sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MILLESIME character Pour filtrer selon la valeur de MILLESIME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MILLESIME sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param TYPE character Pour filtrer selon la valeur de TYPE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour TYPE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LEVEL character Pour filtrer selon la valeur de LEVEL. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LEVEL sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE_MILLESIME character Pour filtrer selon la valeur de CODE_MILLESIME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE_MILLESIME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE_CURRENT character Pour filtrer selon la valeur de CODE_CURRENT. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE_CURRENT sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param OFFICIAL_NAME character Pour filtrer selon la valeur de OFFICIAL_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour OFFICIAL_NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param UPPER_NAME character Pour filtrer selon la valeur de UPPER_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour UPPER_NAME sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LOWER_NAME character Pour filtrer selon la valeur de LOWER_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOWER_NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LONGITUDE character Pour filtrer selon la valeur de LONGITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LONGITUDE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LATITUDE character Pour filtrer selon la valeur de LATITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LATITUDE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LOCATION character Pour filtrer selon la valeur de LOCATION. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOCATION sont: near, within (optional)
  # @param SHAPE character Pour filtrer selon la valeur de SHAPE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour SHAPE sont: near, within (optional)
  # @return [PaginateListCog]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("PaginateReferentielGeozones", {
  # tests for PaginateReferentielGeozones
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Pagination \&quot;referentiel - geozones\&quot;
  # Permet d&#39;obtenir une liste paginée d&#39;éléments de \&quot;referentiel - geozones\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param page integer Numéro de la page à afficher
  # @param page.size character Nombre de lignes par page
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: ID, LEVEL, CODE, VALIDITY_START, VALIDITY_END, NAME (optional)
  # @param ID character Pour filtrer selon la valeur de ID. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ID sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LEVEL character Pour filtrer selon la valeur de LEVEL. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LEVEL sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param VALIDITY_START character Pour filtrer selon la valeur de VALIDITY_START. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_START sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param VALIDITY_END character Pour filtrer selon la valeur de VALIDITY_END. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_END sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME character Pour filtrer selon la valeur de NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @return [PaginateListGeozones]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("PaginateReferentielPolluantsEau", {
  # tests for PaginateReferentielPolluantsEau
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Pagination \&quot;referentiel - polluantsEau\&quot;
  # Permet d&#39;obtenir une liste paginée d&#39;éléments de \&quot;referentiel - polluantsEau\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param page integer Numéro de la page à afficher
  # @param page.size character Nombre de lignes par page
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, NAME, UNIT (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME character Pour filtrer selon la valeur de NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param UNIT character Pour filtrer selon la valeur de UNIT. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour UNIT sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @return [PaginateListPolluantsEau]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("PaginateReferentielPorts", {
  # tests for PaginateReferentielPorts
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Pagination \&quot;referentiel - ports\&quot;
  # Permet d&#39;obtenir une liste paginée d&#39;éléments de \&quot;referentiel - ports\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param page integer Numéro de la page à afficher
  # @param page.size character Nombre de lignes par page
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, NAME, MCA_CODE, MCA_NAME (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME character Pour filtrer selon la valeur de NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MCA_CODE character Pour filtrer selon la valeur de MCA_CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MCA_CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MCA_NAME character Pour filtrer selon la valeur de MCA_NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MCA_NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @return [PaginateListPorts]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("PaginateReferentielStationsAir", {
  # tests for PaginateReferentielStationsAir
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Pagination \&quot;referentiel - stationsAir\&quot;
  # Permet d&#39;obtenir une liste paginée d&#39;éléments de \&quot;referentiel - stationsAir\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param page integer Numéro de la page à afficher
  # @param page.size character Nombre de lignes par page
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, NAME, CODE_ZAS, NAME_ZAS, COMMUNE, AASQA, LONGITUDE, LATITUDE, ALTITUDE, VALIDITY_START, VALIDITY_END, SECTOR_TYPE, MILLESIME, IMPACT_PM10, IMPACT_NO2, IMPACT_O3, IMPACT_SO2, IMPACT_PM25, IMPACT_CO (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME character Pour filtrer selon la valeur de NAME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param CODE_ZAS character Pour filtrer selon la valeur de CODE_ZAS. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE_ZAS sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param NAME_ZAS character Pour filtrer selon la valeur de NAME_ZAS. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour NAME_ZAS sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param COMMUNE character Pour filtrer selon la valeur de COMMUNE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour COMMUNE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param AASQA character Pour filtrer selon la valeur de AASQA. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour AASQA sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LONGITUDE character Pour filtrer selon la valeur de LONGITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LONGITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LATITUDE character Pour filtrer selon la valeur de LATITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LATITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param ALTITUDE character Pour filtrer selon la valeur de ALTITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ALTITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param VALIDITY_START character Pour filtrer selon la valeur de VALIDITY_START. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_START sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param VALIDITY_END character Pour filtrer selon la valeur de VALIDITY_END. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALIDITY_END sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param SECTOR_TYPE character Pour filtrer selon la valeur de SECTOR_TYPE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour SECTOR_TYPE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param MILLESIME character Pour filtrer selon la valeur de MILLESIME. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour MILLESIME sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param impact.pm10 character Pour filtrer selon la valeur de IMPACT_PM10. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_PM10 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.no2 character Pour filtrer selon la valeur de IMPACT_NO2. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_NO2 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.o3 character Pour filtrer selon la valeur de IMPACT_O3. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_O3 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.so2 character Pour filtrer selon la valeur de IMPACT_SO2. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_SO2 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param impact.pm25 character Pour filtrer selon la valeur de IMPACT_PM25. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_PM25 sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param IMPACT_CO character Pour filtrer selon la valeur de IMPACT_CO. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour IMPACT_CO sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LOCATION character Pour filtrer selon la valeur de LOCATION. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOCATION sont: near, within (optional)
  # @return [PaginateListStationsAir]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("PaginateReferentielStationsEsu", {
  # tests for PaginateReferentielStationsEsu
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Pagination \&quot;referentiel - stationsEsu\&quot;
  # Permet d&#39;obtenir une liste paginée d&#39;éléments de \&quot;referentiel - stationsEsu\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param page integer Numéro de la page à afficher
  # @param page.size character Nombre de lignes par page
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: CODE, LONGITUDE, LATITUDE (optional)
  # @param CODE character Pour filtrer selon la valeur de CODE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour CODE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param LONGITUDE character Pour filtrer selon la valeur de LONGITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LONGITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LATITUDE character Pour filtrer selon la valeur de LATITUDE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LATITUDE sont: eq, ne, gt, gte, lt, lte, in, nin (optional)
  # @param LOCATION character Pour filtrer selon la valeur de LOCATION. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour LOCATION sont: near, within (optional)
  # @return [PaginateListStationsEsu]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("PaginateReferentielTags", {
  # tests for PaginateReferentielTags
  # base path: https://data.statistiques.developpement-durable.gouv.fr/dido/api
  # Pagination \&quot;referentiel - tags\&quot;
  # Permet d&#39;obtenir une liste paginée d&#39;éléments de \&quot;referentiel - tags\&quot;. Il est possible de trier, filtrer et sélectionner les colonnes souhaitées.
  # @param page integer Numéro de la page à afficher
  # @param page.size character Nombre de lignes par page
  # @param order.by array[character] Options de tri (optional)
  # @param columns array[character] Liste des colonnes à inclure dans le résultat. Les colonnes doivent être saisies en séparant chaque valeur par une virgule. La liste des colonnes disponibles est la suivante: VALUE, DISPLAY, TOPICS, EUROVOC, ECOPLANET (optional)
  # @param VALUE character Pour filtrer selon la valeur de VALUE. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour VALUE sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param DISPLAY character Pour filtrer selon la valeur de DISPLAY. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour DISPLAY sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param TOPICS character Pour filtrer selon la valeur de TOPICS. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour TOPICS sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param EUROVOC character Pour filtrer selon la valeur de EUROVOC. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour EUROVOC sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @param ECOPLANET character Pour filtrer selon la valeur de ECOPLANET. Le format doit être de la forme \"typeFiltre:valeurFiltre\". Les types de filtres disponibles pour ECOPLANET sont: eq, ne, startsWith, endsWith, in, nin (optional)
  # @return [PaginateListTags]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})


# DiDo - API diffusion
#
# API de diffusion des données de DiDo sur Internet
#
# The version of the OpenAPI document: 1.0.0
# Contact: support-dido@developpement-durable.gouv.fr
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Datasets operations
#' @description didoscalie.Datasets
#' @format An \code{R6Class} generator object
#' @field apiClient Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ GetIdDataset } \emph{ Détail d&#39;un jeu de données (identifié par son id) }
#' Permet d&#39;obtenir le détail d&#39;un jeu de données qui est identifié par son id.
#'
#' \itemize{
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } \link{DatasetFull} \cr
#'
#'
#' \item status code : 200 | Réponse avec une le détail d&#39;un jeu de données
#'
#' \item return type : DatasetFull 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Réponse dans le cas d&#39;une erreur de validation
#'
#' \item return type : ValidationError 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Réponse si élément non trouvé
#'
#' \item return type : NotFoundError 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | Réponse si erreur interne au serveur
#'
#' \item return type : ServerError 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PaginateDatasets } \emph{ Pagination des jeux de données }
#' Permet d&#39;obtenir une liste paginée des jeux de données présents dans DiDo. Il est possible de spécifier le nombre de jeux de données à retourner, le numéro de la page à retourner et ajouter des critères de tri et de filtrage.
#'
#' \itemize{
#' \item \emph{ @param } page integer
#' \item \emph{ @param } page.size Enum < [all, 10, 20, 50, 100] > 
#' \item \emph{ @param } order.by Enum < [title, last_modified, -title, -last_modified] > 
#' \item \emph{ @param } text character
#' \item \emph{ @param } topics Enum < [Environnement, Énergie, Transports, Logement, Changement climatique] > 
#' \item \emph{ @param } tags list( character )
#' \item \emph{ @param } licenses list( character )
#' \item \emph{ @param } organizations list( character )
#' \item \emph{ @param } min.last.modified character
#' \item \emph{ @param } max.last.modified character
#' \item \emph{ @returnType } \link{PaginateListDatasets} \cr
#'
#'
#' \item status code : 200 | Réponse avec une liste paginée de jeux de données
#'
#' \item return type : PaginateListDatasets 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Réponse dans le cas d&#39;une erreur de validation
#'
#' \item return type : ValidationError 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | Réponse si erreur interne au serveur
#'
#' \item return type : ServerError 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  GetIdDataset  ####################
#'
#' library(didoscalie)
#' var.id <- 'id_example' # character | Identifiant du jeu de données.
#'
#' #Détail d'un jeu de données (identifié par son id)
#' api.instance <- DatasetsApi$new()
#'
#' result <- api.instance$GetIdDataset(var.id)
#'
#'
#' ####################  PaginateDatasets  ####################
#'
#' library(didoscalie)
#' var.page <- 56 # integer | Numéro de la page à afficher
#' var.page.size <- 'page.size_example' # character | Nombre de lignes par page
#' var.order.by <- ['order.by_example'] # array[character] | Options de tri des jeux de données
#' var.text <- 'text_example' # character | Texte à rechercher dans le titre ou la description du jeu de données
#' var.topics <- ['topics_example'] # array[character] | Permet de filtrer les jeux de données par thème. Le thèmes doivent être séparés par des virgules.
#' var.tags <- ['tags_example'] # array[character] | Permet de filtrer les jeux de données par mot-clé. Le mots-clé doivent être séparés par des virgules.
#' var.licenses <- ['licenses_example'] # array[character] | Permet de filtrer les jeux de données par licence. Les licences doivent être séparées par des virgules.
#' var.organizations <- ['organizations_example'] # array[character] | Permet de filtrer les jeux de données par producteur. Les identifiants de chaque producteur doivent être séparés par des virgules.
#' var.min.last.modified <- 'min.last.modified_example' # character | Date de mise-à-jour minimale pour filtrer les jeux de données (format iso 8601).
#' var.max.last.modified <- 'max.last.modified_example' # character | Date de mise-à-jour maximale pour filtrer les jeux de données (format iso 8601).
#'
#' #Pagination des jeux de données
#' api.instance <- DatasetsApi$new()
#'
#' result <- api.instance$PaginateDatasets(var.page, var.page.size, order.by=var.order.by, text=var.text, topics=var.topics, tags=var.tags, licenses=var.licenses, organizations=var.organizations, min.last.modified=var.min.last.modified, max.last.modified=var.max.last.modified)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
DatasetsApi <- R6::R6Class(
  'DatasetsApi',
  public = list(
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    GetIdDataset = function(id, ...){
      apiResponse <- self$GetIdDatasetWithHttpInfo(id, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    GetIdDatasetWithHttpInfo = function(id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      body <- NULL
      urlPath <- "/v1/datasets/{id}"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), URLencode(as.character(`id`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "DatasetFull", loadNamespace("didoscalie")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    PaginateDatasets = function(page, page.size, order.by=NULL, text=NULL, topics=NULL, tags=NULL, licenses=NULL, organizations=NULL, min.last.modified=NULL, max.last.modified=NULL, ...){
      apiResponse <- self$PaginateDatasetsWithHttpInfo(page, page.size, order.by, text, topics, tags, licenses, organizations, min.last.modified, max.last.modified, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    PaginateDatasetsWithHttpInfo = function(page, page.size, order.by=NULL, text=NULL, topics=NULL, tags=NULL, licenses=NULL, organizations=NULL, min.last.modified=NULL, max.last.modified=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`page`)) {
        stop("Missing required parameter `page`.")
      }

      if (missing(`page.size`)) {
        stop("Missing required parameter `page.size`.")
      }

      queryParams['page'] <- page

      queryParams['pageSize'] <- page.size

      queryParams['orderBy'] <- order.by

      queryParams['text'] <- text

      queryParams['topics'] <- topics

      queryParams['tags'] <- tags

      queryParams['licenses'] <- licenses

      queryParams['organizations'] <- organizations

      queryParams['minLastModified'] <- min.last.modified

      queryParams['maxLastModified'] <- max.last.modified

      body <- NULL
      urlPath <- "/v1/datasets"

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "PaginateListDatasets", loadNamespace("didoscalie")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    }
  )
)
